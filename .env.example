# WebSocket 服务端口
PORT=8001

# SSL 证书路径 (绝对路径或相对项目根目录的相对路径)
# 生产环境必填；本地调试可留空并设置 USE_HTTP=1
# 示例: /etc/letsencrypt/live/your-domain/privkey.pem
SSL_KEY_PATH=
SSL_CERT_PATH=

# 本地调试模式: USE_HTTP=1 跳过 SSL，使用 HTTP 启动
# 生产环境保持注释或设为 0
# USE_HTTP=1

# 运行环境
NODE_ENV=development
